{{#partial "content"}}
    <p>Team Name: {{team.name}}</p>
    <p>About: {{team.description}}</p>
    <p>Members:</p>
        <ul>
            {{#each members}}
                <p>{{ name }}</p>
            {{/each}}
        </ul>
    <!--<form action="/teams/{{team.id}}" method="post">-->
        <!--<label for="aMember">Add members</label>-->
        <!--<input id="aMember" name="aMember" type="text">-->
        <!--<button type="submit" class="btn btn-info">Submit</button>-->
    <!--</form>-->

    <br><br>
    <a href="/teams/{{team.id}}/update">Edit this team</a>
    <br>
    <a href="/members/delete">Remove Team Members</a>
    <br>
    <a href="/teams/{{team.id}}/delete">Remove this team</a>
    <br>
    <a href="/">Back</a>
    <br>

{{/partial}}

{{>layout.hbs}}